@model IEnumerable<SampleGlobalization.Models.SimpleFormModel>

@{
    ViewBag.Title = "SimpleFormList";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>SimpleFormList</h2>

<p>
    @Html.ActionLink(Resources.Resource.SimpleFormAdd, "SimpleForm")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nick)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nick)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
      @*  <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>*@
    </tr>
}

</table>
